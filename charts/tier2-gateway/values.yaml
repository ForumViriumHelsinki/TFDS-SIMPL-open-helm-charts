server-port: &server-port 443
management-port: &management-port 8080
replicaCount: 1
image:
  repository: ${CI_REGISTRY_IMAGE}
  pullPolicy: Always
  tag: 2.11.4
imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  runAsUser: 1001


service:
  type: LoadBalancer
  port: *server-port

managementService:
  enabled: false
  type: ClusterIP
  port: *management-port

resources: 
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 1Gi

livenessProbe:
  httpGet:
    path: "/actuator/health/liveness"
    port: *management-port
  initialDelaySeconds: 60
  periodSeconds: 30
  failureThreshold: 10
readinessProbe:
  httpGet:
    path: "/actuator/health/readiness"
    port: *management-port
  initialDelaySeconds: 60
  periodSeconds: 30
  failureThreshold: 10

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes:
  - name: spring-config
    configMap:
      name: tier2-gateway-spring-configmap
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts:
  - name: spring-config
    mountPath: /config/

envFrom:
  - configMapRef:
      name: tier2-gateway-env-configmap

nodeSelector: {}

tolerations: []

affinity: {}

# App configuration, this is only an example! Follow the readme in the installation guide.
#appConfig:
#  external-routes:
#    spring-route-definitions:
#      - id: service-name
#        uri: service-url
#        predicates:
#          - Path=/serviceApi/**
#        filters:
#          - StripPrefix=1
#
#    logging:
#      business:
#        - method: POST
#          path: /serviceApi/endpoint
#          config:
#            operations:
#              - BUSINESS_ACTION_NAME
#    public-urls:
#      - method: GET
#        path: "/v1/info/version"
#    denied-urls:
#      - method: GET
#        path: "/path/to/deny"
#    abac:
#      - method: GET
#        path: "/path/to/protect"
#        identity-attributes:
#          - CONSUMER
#      - path: "/other/path/to/protect"
#        identity-attributes:
#          - DATA_PROVIDER
#  management:
#    tracing:
#      sampling:
#        probability: [MANAGEMENT_TRACING_SAMPLING_PROBABILITY]
#    otlp:
#      tracing:
#        endpoint: [MANAGEMENT_OTLP_TRACING_ENDPOINT]

appConfig: {}

#Kafka configuration, this is only an example!
#kafkaConfig:
#  spring:
#    kafka:
#      bootstrap-servers: <your-kafka-url>
#      consumer:
#        properties:
#          security.protocol: SASL_PLAINTEXT
#          sasl.mechanism: PLAIN
#          sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="${KAFKA_USER:admin}" password="${KAFKA_PASSWORD:admin-secret}";
#      admin:
#        auto-create: true
#  simpl:
#    kafka:
#      topic:
#        prefix: [SIMPL_KAFKA_TOPIC_PREFIX]

kafkaConfig: {}

profile: "" # authority or participant

#path of extra config to add to the deploy, separated by comma
extraConfigImport: /config/kafka.yml,classpath:config/routes-{{ .Values.profile }}.yml

microservices:
  usersRolesUrl: http://users-roles.{{ .Release.Namespace }}.svc.cluster.local:8080
  securityAttributesProviderUrl: http://security-attributes-provider.{{ .Release.Namespace }}.svc.cluster.local:8080
  identityProviderUrl: http://identity-provider.{{ .Release.Namespace }}.svc.cluster.local:8080
  authenticationProviderUrl: http://authentication-provider.{{ .Release.Namespace }}.svc.cluster.local:8080

redis:
  host: "redis-master.{{ .Release.Namespace }}.svc.cluster.local"
  port: "6379"
  username: "default"
  password: "admin"

hostT2: "your-t2-host"
authorityUrlT1: "authority-t1-url"
authorityAuthProviderUrl: "{{ .Values.authorityUrlT1 }}/authApi"

keypair:
  algorithm: EC

devMode: false

initContainers:
  checkAuthProvider:
    checkCredentialPresence:
      enabled: true

