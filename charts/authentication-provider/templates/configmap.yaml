apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-configmap
data:
  kafka.yml: |
{{ tpl (toYaml .Values.kafkaConfig) . | indent 4 }}
  vault.yml: |
{{ tpl (toYaml .Values.vaultConfig) . | indent 4 }}
  application.yml: |
{{ tpl (toYaml .Values.appConfig) . | indent 4 }}