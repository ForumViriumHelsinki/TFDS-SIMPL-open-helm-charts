apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.schemaManager.name }}
spec:
  selector:
    app: {{ .Values.deployment.label }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.schemaManager.servicePort }}
      targetPort: {{ .Values.service.schemaManager.containerPort }}


