apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.fuseki.name }}
spec:
  selector:
    app: {{ .Values.statefulset.label }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.fuseki.servicePort }}
      targetPort: {{ .Values.service.fuseki.containerPort }}


